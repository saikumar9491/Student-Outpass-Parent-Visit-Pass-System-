{
  "name": "student-outpass-parent-visit-pass-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm --prefix client run build",
    "start": "npm --prefix client preview"
  }
}
